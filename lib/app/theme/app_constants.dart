import 'package:flutter/material.dart';

const kPadding = 16.0;
const kMargin = 10.0;
const kRadius = 10.0;

const kWidth = SizedBox(
  width: kPadding,
);

const kWidthH = SizedBox(
  width: kPadding / 2,
);

const kHeight = SizedBox(
  height: kPadding,
);
const kHeightH = SizedBox(
  height: kPadding / 2,
);
